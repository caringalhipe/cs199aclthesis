class LinearOrder extends Poset {
    constructor(line, relation, n) {
      this.line = null;
  
      super(relation, n);
      this.line = this.line;
    }
    getLine() {
      return this.line;
    }
  }